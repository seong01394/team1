<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.club.ClubDAOInter"> <!-- SB가 자동으로 구현하여 연동하는 interface -->

  <insert id="create" parameterType="dev.mvc.news.NewsVO">
    INSERT INTO news(newsno, newsadd, newsinfo) 
    VALUES(news_seq.nextval, #{newsadd}, #{newsinfo})
  </insert>
  
  <select id="read" resultType="dev.mvc.news.NewsVO" parameterType="Integer">
    SELECT newsno, newsadd, newsinfo
    FROM news
    WHERE newsno=#{newsno}
  </select>  

  <update id="update" parameterType="dev.mvc.news.NewsVO">
    UPDATE news 
    SET newsadd= # {newsadd}, newsinfo= #{newsinfo}
    WHERE newsno=#{newsno} 
  </update> 

  <delete id="delete" parameterType="int">
    DELETE FROM news WHERE newsno=#{newsno}
  </delete>  
  
   <select id="list_all" resultType="dev.mvc.news.NewsVO">
    SELECT newsno, newsadd, newsinfo
    FROM news
    ORDER BY newsno ASC
  </select>   
  
  
</mapper> 